# Build Baseline

Task 03 initialized a C++20/CMake project with a UI-independent `tgcloud_core` library, bootstrap desktop executable, and CTest smoke test. No third-party dependency or UI toolkit is committed yet; that choice is deferred until core constraints are clearer.
