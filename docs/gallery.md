# Gallery and Media Layer

The gallery catalog keeps media metadata separate from logical cloud-file records and supports paginated listing. Remote Telegram IDs, local cache paths, dimensions, duration, and sync state are retained. Thumbnail decoding and UI presentation belong to later desktop tasks.
