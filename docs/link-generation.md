# `.link` Generation

Single-file generation encrypts the compatibility manifest with the `.link` crypto boundary. Batch generation wraps multiple file manifests in a batch JSON payload before encryption. Output is written incrementally to the destination file.
