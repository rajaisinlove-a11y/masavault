# Multi-bot Scheduler

`BotPool` provides synchronized dynamic assignment of work to enabled bots, round-robin fairness, active counts, assignment totals, and failure counters. It is transport-independent; rate limits and Retry-After policy belong to later Telegram/retry tasks.
