# Setup Wizard and Configuration

The setup model separates storage channel, sync channel, device identity, and bot aliases. Secrets are intentionally represented only by aliases at this UI boundary and must be supplied through secure credential storage; raw tokens/passwords never enter the model or diagnostics.
