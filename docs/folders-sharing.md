# Folder Creation and Select-All Sharing

`FolderTree` provides desktop-oriented folder creation with stable IDs and parent relationships. The sharing selection boundary accepts the visible file IDs as a batch, enabling a UI Select All action to generate one batch share/download request without changing individual file identity. UI wiring is part of the desktop phase.
