# Transfer Center UI Model

The desktop transfer-center model exposes filename, task state, progress snapshot, and active bots to a future native UI. It remains independent of the core transfer engine. A GUI toolkit is intentionally not introduced until the desktop shell task, avoiding a premature dependency choice.
