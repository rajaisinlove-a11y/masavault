# Multi-device Synchronization

`DeviceSync` binds local changes to a stable device identity, records them through the sync log boundary, and tracks the latest applied chain head. Remote transport/replay and conflict application remain explicit later integration layers.
