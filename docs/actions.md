# Share, Backup, and Gallery UI Model

The UI action model supports batch sharing of selected IDs, backup export targets, and gallery filtering. It consumes the core `.link`, backup, and gallery boundaries without duplicating crypto or storage logic. Select All therefore maps to one batch share action while preserving file IDs.
