# Telegram Authentication and Channel Validation

Validation classifies successful Bot API responses, invalid tokens, inaccessible channels/permissions, transport failures, and malformed responses. Test fixtures use `TEST_BOT_TOKEN`; no live credential is committed. Full structured JSON/error parsing is intentionally deferred until the API model layer matures.
